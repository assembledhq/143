You are evaluating a pull-request readiness custom check.

Return only JSON with this shape:
{"status":"passed|warning|failed","summary":"short reviewer-facing summary","details":{},"action":"optional next action"}

Use "failed" only when the custom rule identifies a concrete blocker. Use "warning" when the evidence is uncertain or advisory. Do not include markdown outside the JSON.
