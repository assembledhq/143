---
title: Preview environments
description: Configure live previews so reviewers can inspect frontend and full-stack changes from a session.
section: Guides
order: 12
status: stable
audience: engineer
tags:
  - previews
  - repo-config
  - frontend
llm_summary: Configure the `preview` section of `.143/config.json` so 143 can start services, check readiness, and expose a live session preview.
---

# Preview environments

A preview is a live view of your app running inside a 143 session. When an agent edits a UI, reviewers can inspect the result next to the transcript and diff.

Previews are configured in `.143/config.json` under the top-level `preview` key. Commit that file to the repository so sessions, branch previews, and API-triggered previews all read the same contract.

## Set up the config

Start with the smallest shape that can boot your app:

<Steps>
  <Step>Create `.143/config.json` at the repository root.</Step>
  <Step>Declare one service with a command, port, readiness path, credentials policy, and network policy.</Step>
  <Step>Commit the file to the base branch.</Step>
  <Step>Open a session and use Start Preview, or create a branch preview from the Previews page.</Step>
</Steps>

Use this single-service config for a frontend app with one dev server:

```json
{
  "preview": {
    "name": "frontend",
    "command": ["npm", "run", "dev"],
    "cwd": "frontend",
    "port": 3000,
    "ready": { "http_path": "/", "timeout_seconds": 120 },
    "credentials": { "mode": "none" },
    "network": { "mode": "managed" }
  }
}
```

Notes:

- `command` is argv, not a shell string. Use `["sh", "-c", "..."]` when you need shell features.
- `cwd` is relative to the repository root.
- `port` is the port inside the sandbox. The public preview URL is generated by 143.
- `ready.http_path` should return a 2xx or 3xx response when the app is ready.
- `credentials: { "mode": "none" }` is the right default until the app truly needs secrets.
- `network: { "mode": "managed" }` keeps sandbox egress on the platform-managed policy.

## Full-stack preview config

Use services when the app needs a frontend and backend:

```json
{
  "preview": {
    "name": "full-stack",
    "primary": "frontend",
    "services": {
      "frontend": {
        "command": ["npm", "run", "dev"],
        "cwd": "frontend",
        "port": 3000,
        "env": { "API_URL": "http://localhost:8080" },
        "ready": { "http_path": "/" }
      },
      "server": {
        "command": ["go", "run", "./cmd/server"],
        "port": 8080,
        "ready": { "http_path": "/healthz" }
      }
    },
    "credentials": { "mode": "none" },
    "network": { "mode": "managed" }
  }
}
```

Services share the same sandbox filesystem and `localhost` network namespace, so the frontend can call the backend at `http://localhost:8080`.

## Multiple preview configs

If a repository has more than one previewable app, use named configs:

```json
{
  "preview": {
    "default": "web",
    "configs": {
      "web": {
        "name": "Web app",
        "command": ["npm", "run", "dev"],
        "cwd": "apps/web",
        "port": 3000,
        "ready": { "http_path": "/" },
        "credentials": { "mode": "none" },
        "network": { "mode": "managed" }
      },
      "docs": {
        "name": "Docs",
        "command": ["npm", "run", "dev"],
        "cwd": "apps/docs",
        "port": 3001,
        "ready": { "http_path": "/" },
        "credentials": { "mode": "none" },
        "network": { "mode": "managed" }
      }
    }
  }
}
```

The Previews page can auto-select the default config or let you choose a named config. For scripts, pass the config name with `--config`.

## Add dependencies

Use `preview.install` when preview services need dependencies installed before they boot. This keeps package-manager work out of long service commands and lets 143 cache successful installs.

```json
{
  "preview": {
    "install": {
      "command": ["npm", "ci", "--no-audit", "--no-fund"],
      "lockfiles": ["package-lock.json"],
      "clean_paths": ["node_modules"],
      "verify_paths": ["node_modules/.bin/next"],
      "timeout_seconds": 420
    },
    "name": "frontend",
    "command": ["npm", "run", "dev"],
    "port": 3000,
    "ready": { "http_path": "/" },
    "credentials": { "mode": "none" },
    "network": { "mode": "managed" }
  }
}
```

143 reruns the install when the config or declared lockfiles change, or when a declared verify path is missing.

## Add local infrastructure

Prefer platform-managed infrastructure for preview-only databases and caches. The credentials are generated per preview, injected as environment variables, and destroyed with the preview.

```json
{
  "preview": {
    "name": "full-stack",
    "primary": "frontend",
    "services": {
      "frontend": {
        "command": ["npm", "run", "dev"],
        "cwd": "frontend",
        "port": 3000,
        "env": { "API_URL": "http://localhost:8080" },
        "ready": { "http_path": "/" }
      },
      "server": {
        "command": ["go", "run", "./cmd/server"],
        "port": 8080,
        "ready": { "http_path": "/healthz" }
      }
    },
    "infrastructure": {
      "db": {
        "template": "postgres-17",
        "inject_env": {
          "DATABASE_URL": "postgres://{{username}}:{{password}}@{{host}}:{{port}}/{{database}}?sslmode=disable"
        },
        "inject_into": ["server"]
      }
    },
    "credentials": { "mode": "none" },
    "network": { "mode": "managed" }
  }
}
```

Supported placeholders in `inject_env` are `{{username}}`, `{{password}}`, `{{host}}`, `{{port}}`, and `{{database}}`.

## Secrets and config

Do not put API keys, database URLs, or tokens in `.143/config.json`. Put non-secret values in `preview.services.<service>.env`; use `preview.credentials` only when the preview needs a managed credential set.

```json
{
  "preview": {
    "credentials": {
      "mode": "managed_env",
      "credential_set": "repo-staging",
      "env": ["DATABASE_URL", "STRIPE_SECRET_KEY"],
      "inject_into": ["server"]
    },
    "network": {
      "mode": "managed",
      "destinations": ["staging_db"]
    }
  }
}
```

In this model, an admin creates the managed credential set outside the repo. The config only references the set by name and allowlists which environment variables may be injected. Secret values are not committed to git and are injected at process start.

Scope secrets to the smallest service list with `inject_into`. A frontend service usually should not receive server-only secrets because public frontend frameworks can expose environment variables in browser bundles.

<Callout type="warning" title="Connected previews">
Any preview with `preview.credentials.mode` other than `none`, or with `preview.network.destinations`, is treated as connected. Connected previews pin launch behavior to the base branch so a session diff cannot change commands, ports, or env handling while secrets are in scope.
</Callout>

## How readiness works

143 starts declared services and waits for their readiness probes. A preview becomes available only after the primary service is reachable.

<Steps>
  <Step>143 reads `.143/config.json` from the repo.</Step>
  <Step>It starts infrastructure and services in dependency order.</Step>
  <Step>Each service must pass its readiness probe.</Step>
  <Step>The session page exposes the preview on an isolated preview origin.</Step>
</Steps>

Every service also receives `HOST=0.0.0.0` and, when available, `PREVIEW_ORIGIN`. Use `PREVIEW_ORIGIN` as the external base URL for callbacks, redirects, and absolute links that must point back to the public preview URL.

## Use previews

Use previews from:

- A session: click Start Preview after the sandbox is available.
- A branch: go to Previews, choose repository and branch, then start a preview.
- CI or scripts: create a scoped preview API token in Settings, then call the preview API or the bundled `143 preview create` command with `--repo`, `--branch`, and optional `--commit-sha`.

Previews are most useful for UI changes, full-stack workflow changes, and review paths where the correct result is easier to see than infer from a diff.

## Troubleshooting

| Symptom | What to check |
|---|---|
| No preview button or `PREVIEW_NO_CONFIG` | `.143/config.json` is missing or has no `preview` section. |
| `primary does not reference a service` | `preview.primary` must match a key in `preview.services`. |
| Readiness timeout | Confirm the service binds to `0.0.0.0`, listens on the declared `port`, and returns 2xx or 3xx on `ready.http_path`. |
| Secret missing in a service | Check `credential_set`, the `env` allowlist, and `inject_into`. |
| Preview points at localhost in redirects | Use `PREVIEW_ORIGIN` for the app's external base URL. |

## References

- [Repo config guide](/docs/guides/repo-config): where `.143/config.json` fits with bootstrap and validation.
- [Preview config reference](/docs/reference/preview-config): every supported preview field and constraint.
- [Review and ship](/docs/getting-started/review-and-ship): how previews fit into review workflows.
