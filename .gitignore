# If you prefer the allow list template instead of the deny list, see community template:
# https://github.com/github/gitignore/blob/main/community/Golang/Go.AllowList.gitignore
#
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Code coverage profiles and other test artifacts
*.out
coverage.*
*.coverprofile
profile.cov

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

# env files (never commit secrets — plaintext or encrypted)
.env
.env.local
.env.pre-ngrok
.env.staging
.env.production
.env.*.local
# SOPS-encrypted bundles live in the private secrets repo (../143-infra),
# never here: ciphertext published in a public repo can't be unpublished
# if a decryption key ever leaks. Also covers the transient staging copies
# (.env*.enc.XXXXXX) from sync-static-egress-secrets.sh.
.env*.enc
.env*.enc.*
# CI checkout location of the private secrets repo (see deploy.yml)
.infra/

# air live reload
tmp/

# Build output
bin/
sandbox/.build-stamp

# Runtime data (sandbox snapshots, etc.)
.data/
server
!cmd/server/
!cmd/server/session_executor_main.go
coverage.html

# Root-level `go build` artifacts, one per cmd/ package (rooted so the
# cmd/* source directories stay tracked)
/preview
/tools
/migrate
/worker-deployctl
/mcp
/backfill-usage
/deploy-guardrail
/lint-schema
/lint-stores

# Go module/build caches (if created locally)
.gomodcache/
.gopath/

# Node modules
node_modules/

# Editor/IDE
# .idea/
# .vscode/

# Claude local settings
.claude/settings.local.json
.claude/worktrees/

# 143-tools CLI cross-compile output (make build-cli)
dist/
