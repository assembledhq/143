# Fleet host definitions for deploy-fleet.sh
# Format: role IP (one per line)
# Lines starting with # are ignored.
#
# Usage:
#   cp deploy/fleet-hosts.txt.example deploy/fleet-hosts.txt
#   # Edit with your actual IPs, then:
#   make deploy-fleet SSH_KEY=~/.ssh/143-deploy

db     10.0.0.3
app    10.0.0.2
worker 10.0.0.4
